---
title: C6214
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6214
helpviewer_keywords:
- C6214
ms.assetid: 233e2395-61c1-4a3b-a54b-f19a9ffc31a8
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 18fb7bdd3aacd7d0dee79c153d43839dc3fa43c0
ms.sourcegitcommit: 485ffaedb1ade71490f11cf05962add1718945cc
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 10/16/2019
ms.locfileid: "72441838"
---
# <a name="c6214"></a>C6214

AVERTISSEMENT C6214 : cast entre types d’entiers sémantiquement différents : HRESULT vers un type booléen

Cet avertissement indique qu’un `HRESULT` est casté en un type booléen. La valeur de succès (`S_OK`) d’une `HRESULT` est égale à 0. Toutefois, 0 indique un échec pour un type booléen. Le fait d’effectuer un cast d’un `HRESULT` en un type booléen, puis de l’utiliser dans une expression de test produit un résultat incorrect. Parfois, cet avertissement se produit si un `HRESULT` est stocké dans une variable booléenne. Toute comparaison qui utilise la variable booléenne pour tester la réussite ou l’échec de `HRESULT` peut entraîner des résultats incorrects.

## <a name="example"></a>Exemple

Le code suivant génère cet avertissement :

```cpp
#include <windows.h>

BOOL f( )
{
  HRESULT hr;
  LPMALLOC pMalloc;
  hr = CoGetMalloc(1, &pMalloc);
  if ((BOOL)hr) // warning 6214
  {
    // success code ...
    return TRUE;
  }
  else
  {
    // failure code ...
    return FALSE;
  }
}
```

Pour corriger cet avertissement, utilisez le code suivant :

```cpp
#include <windows.h>

BOOL f( )
{
  HRESULT hr;
  LPMALLOC pMalloc;

  hr = CoGetMalloc(1, &pMalloc);
  if (SUCCEEDED(hr))
  {
    // success code ...
    return TRUE;
  }
  else
  {
    // failure code ...
    return FALSE;
  }
}
```

Pour cet avertissement, le type `SCODE` équivaut à `HRESULT`.

En règle générale, la macro `SUCCEEDED` ou `FAILED` doit être utilisée pour tester la valeur d’un `HRESULT`.

Pour plus d'informations, consultez l'une des rubriques suivantes :

[A réussi](/windows/desktop/api/winerror/nf-winerror-succeeded)

[DÉFECTUEUX](/windows/desktop/api/winerror/nf-winerror-failed)

Pour tirer parti de C++ la méthodologie d’allocation de mémoire moderne, utilisez les mécanismes fournis C++ par la bibliothèque STL (Standard Template Library). Il s’agit notamment de [shared_ptr](/cpp/standard-library/shared-ptr-class), [unique_ptr](/cpp/standard-library/unique-ptr-class)et [Vector](/cpp/standard-library/vector). Pour plus d’informations, consultez [pointeurs intelligents](/cpp/cpp/smart-pointers-modern-cpp) et [ C++ bibliothèque standard](/cpp/standard-library/cpp-standard-library-reference).
