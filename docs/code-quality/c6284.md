---
title: C6284
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6284
helpviewer_keywords:
- C6284
ms.assetid: f3633df6-2978-4899-8c0b-b495bd869e1a
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 8c553a5b75c073e9f79a3856c052313009c267a8
ms.sourcegitcommit: 174c992ecdc868ecbf7d3cee654bbc2855aeb67d
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 12/06/2019
ms.locfileid: "74878357"
---
# <a name="c6284"></a>C6284
AVERTISSEMENT C6284 : objet passé en tant que paramètre'% d’quand une chaîne est requise dans l’appel à \<fonction >.

 Cet avertissement indique que la chaîne de format spécifie une chaîne, par exemple, une spécification `%s` pour `printf` ou `scanf`, C++ mais qu’un objet a été passé à la place.

 Ce défaut peut produire une sortie ou des blocages incorrects.

 Ce message est souvent signalé en raison du passage C++ d’un objet qui implémente un type de chaîne, par exemple, `std::string`, `CComBSTR` ou `bstr_t`, dans un appel de type `printf`C. En fonction de l’implémentation de la C++ classe, autrement dit, si les opérateurs de cast appropriés sont définis C++ , les objets String peuvent souvent être utilisés de manière transparente à chaque fois que des chaînes C sont requises ; Toutefois, étant donné que les paramètres des fonctions de style `printf`sont essentiellement non typés, aucune conversion en chaîne ne se produit.

 En fonction de l’objet, il peut être approprié d’insérer un `static_cast` opérateur dans le type de chaîne approprié, par exemple, `char *` ou `TCHAR``*`, ou d’appeler une fonction membre qui retourne une chaîne, par exemple, `c_str()`, sur des instances de `std::string`.

## <a name="example"></a>Exemple
 Le code suivant génère cet avertissement, car un `CComBSTR` est passé à la fonction `sprintf` :

```cpp
#include <atlbase.h>
#include <stdlib.h>

void f()
{
  char buff[50];
  CComBSTR bstrValue("Bye");

  sprintf(buff,"%ws",bstrValue);
}
```

 Le code suivant utilise un cast statique pour corriger cet avertissement :

```cpp
#include <atlbase.h>
#include <stdlib.h>

void f()
{
  char buff[50];
  CComBSTR bstrValue("Bye");

  sprintf_s(buff,50,"%ws",static_cast<wchar_t *>(bstrValue));
}
```

## <a name="see-also"></a>Voir aussi

- [static_cast, opérateur](/cpp/cpp/static-cast-operator)
- [sprintf_s, _sprintf_s_l, swprintf_s, _swprintf_s_l](/cpp/c-runtime-library/reference/sprintf-s-sprintf-s-l-swprintf-s-swprintf-s-l)
